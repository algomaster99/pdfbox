<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.apache.pdfbox</groupId>
    <artifactId>pdfbox-parent</artifactId>
    <version>3.0.7</version>
    <relativePath>../parent/pom.xml</relativePath>
  </parent>
  <artifactId>xmpbox</artifactId>
  <packaging>bundle</packaging>
  <name>Apache XmpBox</name>
  <description>The Apache XmpBox library is an open source Java tool that implements Adobe's XMP(TM)
    specification. It can be used to parse, validate and create xmp contents.
    It is mainly used by subproject preflight of Apache PDFBox. 
    XmpBox is a subproject of Apache PDFBox.</description>
  <scm>
    <connection>scm:svn:https://svn.apache.org/repos/asf/pdfbox/tags/3.0.7/xmpbox</connection>
    <developerConnection>scm:svn:https://svn.apache.org/repos/asf/pdfbox/tags/3.0.7/xmpbox</developerConnection>
    <url>https://svn.apache.org/viewvc/pdfbox/tags/3.0.7/xmpbox</url>
  </scm>
  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
      <version>${commons-logging.version}</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Automatic-Module-Name>org.apache.xmpbox</Automatic-Module-Name>
          </instructions>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.rat</groupId>
        <artifactId>apache-rat-plugin</artifactId>
        <configuration>
          <inputExcludes>
            <inputExclude>src/test/resources/org/apache/xmpbox/xml/PDFBOX-3882-dematbox.xml</inputExclude>
            <inputExclude>src/test/resources/org/apache/xmpbox/xml/PDFBOX-6131-0015675.xml</inputExclude>
            <inputExclude>src/test/resources/org/apache/xmpbox/xml/PDFBOX-6131-RMR6DEEUWZO6IM3A7WKRPX33SZMBTTQZ.xml</inputExclude>
            <inputExclude>src/test/resources/org/apache/xmpbox/xml/PDFBOX-6133-0064638.xml</inputExclude>
          </inputExcludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>iterative-merge</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <argLine>-XX:AOTMode=merge -XX:AOTCache=../fontbox/cache.aot -XX:AOTCacheOutput=cache.aot</argLine>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
